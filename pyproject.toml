[build-system]
requires = ["setuptools == 68.2.2", "numpy","scipy","matplotlib"]
build-backend = "setuptools.build_meta"

[project]
name = "empmap"
authors = [{name = "Zeke Piskulich", email = "piskulichz@gmail.com"}]
dynamic = ["version", "description"]